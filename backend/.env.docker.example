# Docker Environment Variables
# Copy this to .env.docker and add your actual API key

# Required: Anthropic API Key for all AI agents
ANTHROPIC_API_KEY=sk-ant-your-actual-api-key-here

# Optional: OpenAI API Key (not used with current setup)
OPENAI_API_KEY=

# Database (automatically configured in docker-compose.yml)
# DATABASE_URL=postgresql+asyncpg://postgres:password@postgres:5432/krilin_ai

# Redis (automatically configured in docker-compose.yml)
# REDIS_URL=redis://redis:6379/0

# Security (change in production!)
# SECRET_KEY=your-production-secret-key
